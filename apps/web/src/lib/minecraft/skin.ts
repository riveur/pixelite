export function getBustUrl(uuid: string) {
  return `https://skins.mcstats.com/bust/${uuid}?disableCosmeticType=all`
}

export function getSkullUrl(uuid: string) {
  return `https://skins.mcstats.com/skull/${uuid}?disableCosmeticType=all`
}
